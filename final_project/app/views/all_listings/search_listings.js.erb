
$("ul.search_list").html("")

<% @listings.each do |listing| %>
  $("ul.search_list").append("<%= escape_javascript(render('all_listings/listings_partial', a_listing: listing )) %>")
<% end %>
$(".hidetable").hide()
$("ul.search_list").prepend("<%= escape_javascript(render('all_listings/sorter')) %>")
